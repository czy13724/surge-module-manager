# 这些环境变量需要在 Vercel 和 GitHub 中设置，不要在代码中保存！
# NEXTAUTH_URL=你的域名（生产环境）或 http://localhost:3000（开发环境）
# NEXTAUTH_SECRET=使用 openssl rand -base64 32 生成
# GITHUB_ID=在 GitHub OAuth App 设置中获取
# GITHUB_SECRET=在 GitHub OAuth App 设置中获取
