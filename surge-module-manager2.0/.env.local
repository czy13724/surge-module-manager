# 这些环境变量需要在 Vercel 和 GitHub 中设置，不要在代码中保存！
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret # 使用 openssl rand -base64 32 生成
GITHUB_ID=your-github-client-id
GITHUB_SECRET=your-github-client-secret
